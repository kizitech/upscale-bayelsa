

export interface ServiceCardType {
  title: string;
  description: string;
  bgColor: string;
  cardColor: string;
  textColor: string;
  buttonColor: string;
  buttonTextColor: string;
  themeColor: string;
  path: React.ReactNode;
  image: string;
}

export interface StatType {
  value: string;
  label: string;
}
