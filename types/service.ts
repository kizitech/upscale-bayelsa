export interface ServiceCardType {
  title: string;
  bgColor: string;
  cardColor: string;
  textColor: string;
  buttonColor: string;
  buttonTextColor: string;
  themeColor: string;
  path: React.ReactNode;
}

export interface StatType {
  value: string;
  label: string;
}
